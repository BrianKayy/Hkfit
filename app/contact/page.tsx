"use client";
import { FormEvent, useState } from "react";
export default function ContactPage(){const [status,setStatus]=useState("");function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();const data=new FormData(e.currentTarget);const subject=encodeURIComponent(String(data.get("topic")));const body=encodeURIComponent(`Name: ${data.get("name")}\nEmail: ${data.get("email")}\n\n${data.get("message")}`);window.location.href=`mailto:hello@hkfitness.com?subject=${subject}&body=${body}`;setStatus("Your email app will open with your enquiry. Send it there to reach our team.");}return <main className="editorial-page"><p className="eyebrow">Customer care</p><h1>At your <em>service.</em></h1><p>For questions about a piece, sizing, or your shopping experience, get in touch at <a className="text-link" href="mailto:hello@hkfitness.com">hello@hkfitness.com</a>.</p><form style={{maxWidth:620,marginTop:40}} onSubmit={submit}><div className="form-field"><label htmlFor="name">Full name</label><input id="name" name="name" autoComplete="name" required maxLength={150}/></div><div className="form-field"><label htmlFor="email">Email address</label><input id="email" name="email" type="email" autoComplete="email" required/></div><div className="form-field"><label htmlFor="topic">How can we help?</label><select id="topic" name="topic"><option>Product and sizing advice</option><option>Order enquiry</option><option>Delivery and returns</option><option>My account</option><option>General enquiry</option></select></div><div className="form-field"><label htmlFor="message">Your message</label><textarea id="message" name="message" rows={5} required maxLength={4000}/></div><button className="button">Continue to email ↗</button><p className="form-status" role="status">{status}</p></form></main>}
